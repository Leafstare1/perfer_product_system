/*import { createRouter, createWebHistory } from 'vue-router';

const routes = [

  {
    path: '/',
    name: 'Enquiry',
    component: () => import('./router/Enquiry.vue'), 
  }
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
*/